import { DataForCandidates } from './data-for-candidates';

describe('DataForCandidates', () => {
  it('should create an instance', () => {
    expect(new DataForCandidates()).toBeTruthy();
  });
});
